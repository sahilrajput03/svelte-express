<script lang="ts">
	// Please refer `tsconfig.ts`'s "include" key so that typescript/vscode's language server considers files in `common` folder.
	import { variable1 } from '../../../common/utils';
</script>

<h1>Svelte + Expressjs with common files usage</h1>

{variable1}
